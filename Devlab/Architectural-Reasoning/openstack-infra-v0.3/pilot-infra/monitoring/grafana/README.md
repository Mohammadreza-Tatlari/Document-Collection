# Grafana

- Add Prometheus as a data source (URL: http://localhost:9090 or your Prometheus host).
- Import dashboards: Node Exporter (id 1860), Ceph (search "Ceph" in Grafana dashboard import).
- Optionally put dashboard JSON files here and use provisioning to load them automatically.
